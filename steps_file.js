/* eslint-disable */
// Minimal steps_file for CodeceptJS
// This provides the `I` actor. Add custom steps here if needed.
module.exports = function() {
  return actor({
    // define custom steps here, e.g.
    // login(email, password) {
    //   this.fillField('Email', email);
    //   this.fillField('Password', password);
    //   this.click('Sign in');
    // }
  });
};
